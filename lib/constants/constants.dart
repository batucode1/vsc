import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class Sabitler {
  static const arkaPlan = Colors.red;
  static const buttonColor = Colors.red;
  static const yaziRengi = Colors.black;
  static BorderRadius textBorder = BorderRadius.circular(15);
  static const padding = EdgeInsets.all(20);
}
